<?xml version="1.0" encoding="utf-8"?>
<font-family xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!-- regular -->
    <font
        android:font="@font/primary_regular"
        android:fontStyle="normal"
        android:fontWeight="400"

        app:font="@font/primary_regular"
        app:fontStyle="normal"
        app:fontWeight="400" />

    <!-- semi bold -->
    <font
        android:font="@font/primary_semi"
        android:fontStyle="normal"
        android:fontWeight="600"

        app:font="@font/primary_semi"
        app:fontStyle="normal"
        app:fontWeight="600" />

    <!-- bold -->
    <font
        android:font="@font/primary_bold"
        android:fontStyle="normal"
        android:fontWeight="700"

        app:font="@font/primary_bold"
        app:fontStyle="normal"
        app:fontWeight="700" />
</font-family>